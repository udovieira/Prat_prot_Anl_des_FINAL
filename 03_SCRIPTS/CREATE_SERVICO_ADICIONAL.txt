CREATE TABLE `connectwork`.`SERVICO_ADICIONAL` (
    `ID_SERVICO_ADICIONAL` INT NOT NULL,
    `ID_SERVICO` INT NOT NULL,
    `DS_SERVICO_ADICIONAL` VARCHAR(250) NOT NULL,
    PRIMARY KEY (`ID_SERVICO_ADICIONAL`),
    FOREIGN KEY (`ID_SERVICO`)
        REFERENCES `connectwork`.`SERVICO` (`ID_SERVICO`)
);