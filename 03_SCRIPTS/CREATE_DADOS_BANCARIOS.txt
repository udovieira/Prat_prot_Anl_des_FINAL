CREATE TABLE `connectwork`.`DADOS_BANCARIOS` (
    `ID_DADOS_BANCARIOS` INT NOT NULL,
    `NR_TIPO_PAGTO` INT NOT NULL,
    `DS_NOME_TITULAR` VARCHAR(250) NOT NULL,
    `DT_VALIDADE` DATETIME NOT NULL,
    `NR_CVV` INT NOT NULL,
    `FL_SALVAR_CARTAO` INT NOT NULL,
    `FL_CARTAO_PADRAO` INT NOT NULL,
    PRIMARY KEY (`ID_DADOS_BANCARIOS`)
);
