CREATE TABLE `connectwork`.`REMUNERACAO` (
    `ID_REMUNERACAO` INT NOT NULL,
    `ID_CONTRATADO` INT NOT NULL,
    `NR_VL_REMUNERACAO` FLOAT NOT NULL,
    PRIMARY KEY (`ID_REMUNERACAO`),
    FOREIGN KEY (`ID_CONTRATADO`)
        REFERENCES `connectwork`.`CONTRATADO` (`ID_CONTRATADO`)
);
