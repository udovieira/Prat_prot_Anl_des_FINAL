CREATE TABLE `connectwork`.`SERVICO` (
    `ID_SERVICO` INT NOT NULL,
    `DS_TIPO_SERVICO` VARCHAR(250) NOT NULL,
    `DS_GRUPO_SERVICO` VARCHAR(250) NOT NULL,
    `DS_MATERIAL` VARCHAR(250) NOT NULL,
    `DS_STATUS_SERVICO` VARCHAR(250) NOT NULL,
    `DT_INICIO_SERVICO` DATETIME NOT NULL,
    `DT_FIM_SERVICO` DATETIME NOT NULL,
    PRIMARY KEY (`ID_SERVICO`)
);
